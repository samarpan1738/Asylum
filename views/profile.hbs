<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{#each users as |user|}}
    <title>{{user.displayName}}(@{{user.username}})</title>
    {{/each}}
    <link rel="stylesheet" href="/css/homepage.css">
    <link rel="stylesheet" href="/css/user.css">
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    {{!-- <script src="/js/user.js" defer></script> --}}
    <script src="/js/posts.js" defer></script>
    <script src="/js/index.js" defer></script>
    <script src="/js/user.js" defer></script>
    <script defer>
        const loggedInUser = {{{ convert loggedInUser }}}
        const users = {{{ convert users }}}
        const posts = {{{ convert posts }}}
    </script>
</head>

<body>
    <div id="app">
        {{> sidebar}}
        <div class="hero-section">

            {{#each users as |user|}}
            <h3 class="heading">{{user.displayName}}</h3>
            {{!-- <div class="profile">
                <div class="profile-left">
                    <img src="{{user.displayPic}}" alt="Display Pic">
                </div>
                <div class="profile-right">
                    <div class="profile__heading">
                        <h1>{{user.username}}</h1>
                        <button class="follow-btn" class="not-following">Follow</button>
                    </div>
                    <div class="profile__stats">
                        <div><span class="counter">{{user.postCount}} </span><span>posts</span></div>
                        <div><span class="counter">{{user.followerCount}} </span><span>followers</span></div>
                        <div><span class="counter">{{user.followingCount}} </span><span>following</span></div>
                    </div>
                    <h3 class="profile__name">{{user.displayName}}</h3>
                </div>
            </div> --}}
            <div class="profile" id="{{user.username}}">
                <div class="profile-left">
                    <img src="{{user.displayPic}}" alt="Display Pic">
                </div>
                <div class="profile-right">
                    <div class="profile__heading">
                        <h1>{{user.username}}</h1>
                        <button class="follow-btn" class="not-following">Follow</button>
                    </div>
                    <div class="profile__stats">
                        <div><span>{{user.postCount}} </span><span>posts</span></div>
                        <div><span>{{user.followerCount}} </span><span>followers</span></div>
                        <div><span>{{user.followingCount}} </span><span>following</span></div>
                    </div>
                    <h3 class="profile__name">{{user.displayName}}</h3>
                </div>
            </div>
            {{/each}}

            {{> postsContainer}}
        </div>
</body>

</html>